What is a Graph
===============

Some text, pictures, etc.


:api:`tensor.Tensor`


Subtitle
--------

Here is some stuff.

 .. code-block::
  def fib(n):
     if n == 0:
       return 1
     if n == 1:
       return 1
     return fib(n-1) + fib(n-1)

 .. python::
  def fib(n):
     if n == 0:
       return 1
     if n == 1:
       return 1
     return fib(n-1) + fib(n-1)
