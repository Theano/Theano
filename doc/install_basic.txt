.. _install_basic:

Basic user install instructions
===============================

These are the 3 simple ways of obtaining Theano, and additional requirements
will change depending on the way you choose.

pip
~~~
    
The recommended way to obtain the latest stable version of Theano is from PyPI using
pip_. Use the [sudo] or [--user] option depending on your admin rights.

.. code-block:: bash

    [sudo] pip install Theano [--user]

conda
~~~~~

say something about conda and miniconda

Anaconda
~~~~~~~~

If you don't want to bother with anything, Theano can be acquired through
Anaconda_.


.. _pip: http://pypi.python.org/pypi/pip
.. _anaconda: https://docs.continuum.io/

.. _testing_installation:

Testing your installation
~~~~~~~~~~~~~~~~~~~~~~~~~

Once you have installed Theano, you should run the test suite. 

.. code-block:: bash

    THEANO_FLAGS=''; python -c "import theano; theano.test()"

All tests should pass (skipped tests and known failures are normal). If
some test fails on your machine, you are encouraged to tell us what went
wrong on the ``theano-users@googlegroups.com`` mailing list.
